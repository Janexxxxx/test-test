name: 打包应用并上传服务器

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 获取代码
      - name: 迁出代码
        uses: actions/checkout@v4

      # 构建 Docker 镜像
      - name: Build Docker Image
        run: sudo docker build -t nginx:latest .

      - name: 列出构建后的文件
        run: ls -R

      # 发布到腾讯云
      - name: 发布到腾讯云
        uses: easingthemes/ssh-deploy@main
        with:
          # 私钥
          SSH_PRIVATE_KEY: ${{ secrets.PRIVATEKEY }}
          # SCP参数
          ARGS: '-avzr'
          # 服务器ip
          REMOTE_HOST: '49.235.118.67'
          # 用户
          REMOTE_USER: 'root'
          # 测试ssh连接
          SCRIPT_BEFORE: |
            docker stop xj22-nginx || true
            docker rm xj22-nginx || true
            docker run -p 8828:80 --name xj22-nginx -v /xjdata2/nginx/conf:/etc/nginx/conf.d -d nginx:latest
            sleep 5  # 等待 5 秒以确保容器已经正常运行
            docker logs xj22-nginx
